[phases.setup]
nixPkgs = ["nodejs-22_x", "playwright-driver"]

[phases.install]
cmds = ["pnpm install"]

[phases.build]
cmds = [
    "pnpm build",
    "npx playwright install chromium --with-deps"
]

[start]
cmd = "pnpm start"
